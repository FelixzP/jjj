# คัดลอกไฟล์นี้เป็น .env.local แล้วแก้ค่าตามเซิร์ฟเวอร์จริง
# ไฟล์นี้ (.env.local.example) อัปขึ้น GitHub ได้ ส่วนไฟล์ .env.local จริงห้ามอัปเด็ดขาด

# ตอน dev ในเครื่อง localhost ก็พอ
# ตอนขึ้นเซิร์ฟเวอร์จริง เปลี่ยนเป็นโดเมน/IP ของ backend เช่น https://api.yourschool.ac.th
NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_SOCKET_URL=http://localhost:4000
